import 'package:flutter/material.dart';

class UserProvider extends StatelessWidget {
  const UserProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('User Provider Screen')));
  }
}
