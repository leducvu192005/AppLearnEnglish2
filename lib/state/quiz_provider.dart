import 'package:flutter/material.dart';

class QuizProvider extends StatelessWidget {
  const QuizProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Quiz Provider Screen')));
  }
}
