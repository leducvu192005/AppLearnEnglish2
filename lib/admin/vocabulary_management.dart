import 'package:flutter/material.dart';

class VocabularyManagement extends StatelessWidget {
  const VocabularyManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('đây là trang quản lí từ vừng'),
    );
  }
}
